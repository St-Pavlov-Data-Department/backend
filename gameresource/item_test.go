package gameresource

/*
func Test_Item_FromJson(t *testing.T) {
	// Create a new Item instance
	item := &Item{}

	// Test case 1: Valid JSON file
	err := item.LoadFromJson("testdata/items.json")
	assert.NoError(t, err)
	assert.Equal(t, 3, len(item.items))

	// Test case 2: Invalid JSON file
	err = item.LoadFromJson("testdata/invalid.json")
	assert.Error(t, err)
}
*/
